extends ../base
include ../mixins/video.pug

block content
    form(method="GET").search__form
        input(placeholder="Search by Title", name="keyword")
        button 
            i.fas.fa-search
    div.video-grid
        each video in videos 
            +video(video)
        else 
            span.empty__message No Videos Found